package mechanismTests.outtake;

public class OuttakeSlidesTest {
    // this needs low, intermediate, full extension so slides don't slam
    // use PID tuner I guess, this isn't very complicated
    // need states for both low & high basket plus the first bar hang -> values from testing & CAD
    // lock slides & adjust height
}
