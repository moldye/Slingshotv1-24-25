package mechanismTests.outtake;

public class OuttakeTests {
    // Decide this when Souren finishes cad
    // we'll outake any sample using this, even into o zone (that way we don't have to turn robot around)
}
